function About({ title, onButtonClick ,student,friends}) {
  return (
    <div>
      this is about page
      {/* {title}
      <button 
      onClick={() => onButtonClick("user clicked me")}>
        Click me</button><br/>
        {student.name}<br/>
        {friends.join(" | ")} */}
    </div>
  );
}
export default About;
